// TODO: Define Product types and interfaces
// - Product
// - ProductFilters
// - ProductState

export interface Product {
  id: string
  // TODO: Add product fields (name, description, price, etc.)
}

// TODO: Add filter fields (search, category, priceRange, etc.)
// eslint-disable-next-line @typescript-eslint/no-empty-object-type
export interface ProductFilters {}

// TODO: Add state fields (favorites, deleted, created, etc.)
// eslint-disable-next-line @typescript-eslint/no-empty-object-type
export interface ProductState {}
